#let _symbmap = (
  to : "→",
  infty : "∞",
  cdots : "⋯",
  vdots : "⋮",
  int : "∫",
  eq : "≡",
  imp : "⟹",
  iff : "⟺",
  oif : "⟸",
  ex : "∃",
  fa : "∀",
  leq : "≤",
  geq : "≥",
  tick : "✓",
)

#let to = symbol(
  _symbmap.to,
)

#let infty = symbol(
  _symbmap.infty,
)

#let cdots = symbol(
  _symbmap.cdots,
)

#let int = symbol(
  _symbmap.int,
)

#let eq = symbol(
  _symbmap.eq,
)

#let imp = symbol(
  _symbmap.imp
)

#let iff = symbol(
  _symbmap.iff
)

#let oif = symbol(
  _symbmap.oif
)

#let ex = symbol(
  _symbmap.ex
)

#let fa = symbol(
  _symbmap.fa
)

#let vdots = symbol(
  _symbmap.vdots
)

#let leq = symbol(
  _symbmap.leq
)

#let geq = symbol(
  _symbmap.geq
)

#let tick = symbol(
  _symbmap.tick
)
